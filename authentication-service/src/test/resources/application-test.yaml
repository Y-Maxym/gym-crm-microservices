spring:
  application:
    name: authentication-service-test
  cloud:
    discovery:
      enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true